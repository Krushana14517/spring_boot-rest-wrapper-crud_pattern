# spring_boot-rest-wrapper-crud_pattern
A Spring Boot CRUD application for managing student records with built-in request validation. Features a structured Service layer and structured JSON responses via a custom wrapper class.
